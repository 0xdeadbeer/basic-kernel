#ifndef SCREEN_SIZE_H
#define SCREEN_SIZE_H

int x_display = 80; // columns
int y_display = 25; // lines 
int double_ = 2; // double cuz in memory each element is made up of two things,
				 // first is a byte of data, the second one is a byte for the first byte's properties
				 // like color, font family, ..
#endif